# 📤 دليل رفع المشروع على GitHub

## ✅ تم التحضير محلياً!

تم إنشاء Git repository محلياً وحفظ جميع الملفات (18 ملف).

---

## 🚀 الخطوات للرفع على GitHub

### الخطوة 1: إنشاء حساب GitHub (إذا لم يكن لديك)

1. اذهب إلى: https://github.com
2. اضغط **Sign Up**
3. أدخل:
   - Email
   - Password
   - Username (مثلاً: `haithem` أو `yimnak`)
4. أكمل التسجيل

---

### الخطوة 2: إنشاء Repository جديد

1. **بعد تسجيل الدخول** اضغط على **+** (أعلى اليمين)
2. اختر **New repository**

3. **املأ المعلومات:**

   📝 **Repository name:**
   ```
   qudrat-app
   ```

   📝 **Description (اختياري):**
   ```
   دليل القدرات - تطبيق ويب تقدمي لمراجعة اختبار القدرات العامة
   ```

   ☑️ **Public** (اختر هذا لنشر عام)

   ❌ **لا تختر** "Add a README file" (لدينا جاهز)

   ❌ **لا تختر** ".gitignore" (لدينا جاهز)

4. اضغط **Create repository**

---

### الخطوة 3: ربط المشروع المحلي بـ GitHub

**ستظهر لك صفحة بتعليمات.** اتبع هذه الأوامر:

#### افتح Terminal/CMD في مجلد المشروع:

```bash
cd C:\Users\haith\qudrat-app
```

#### نفّذ الأوامر التالية (بالترتيب):

1. **تأكد من Branch name:**
```bash
git branch -M main
```

2. **أضف Remote (استبدل `USERNAME` باسم حسابك على GitHub):**
```bash
git remote add origin https://github.com/USERNAME/qudrat-app.git
```

**مثال:** إذا كان username هو `haithem`:
```bash
git remote add origin https://github.com/haithem/qudrat-app.git
```

3. **ارفع الملفات (Push):**
```bash
git push -u origin main
```

**سيطلب منك:**
- Username: أدخل اسم المستخدم
- Password: أدخل **Personal Access Token** (ليس كلمة المرور)

---

### الخطوة 4: إنشاء Personal Access Token (مهم!)

GitHub لا يقبل كلمة المرور العادية بعد الآن، تحتاج Token:

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **Generate new token** → **Generate new token (classic)**
3. **Note (اسم):** مثلاً `qudrat-app-upload`
4. **Expiration:** اختر `90 days` أو `No expiration`
5. **Select scopes:** اختر:
   - ✅ `repo` (كل الخيارات تحتها)
6. اضغط **Generate token**
7. **انسخ Token** (سيظهر مرة واحدة فقط!)
8. استخدمه كـ Password عند Push

---

### الخطوة 5: التحقق من الرفع

بعد Push الناجح:

1. ارجع لصفحة Repository على GitHub
2. اضغط F5 (Refresh)
3. **ستجد جميع الملفات!** 🎉

---

## 🌐 تفعيل GitHub Pages (للنشر المباشر)

بعد رفع الملفات:

### 1. اذهب إلى Settings

في صفحة Repository:
- اضغط **Settings** (أعلى الصفحة)

### 2. فعّل Pages

- من القائمة الجانبية: **Pages**
- **Source:** اختر `main` branch
- **Folder:** اختر `/ (root)`
- اضغط **Save**

### 3. انتظر دقيقة واحدة

ستظهر رسالة:
```
Your site is published at https://USERNAME.github.io/qudrat-app
```

### 4. افتح الرابط

🎉 **التطبيق الآن على الإنترنت!**

---

## 📱 الخطوات الكاملة (ملخص سريع)

```bash
# 1. في Terminal/CMD
cd C:\Users\haith\qudrat-app

# 2. تأكد من Branch
git branch -M main

# 3. أضف Remote (غيّر USERNAME)
git remote add origin https://github.com/USERNAME/qudrat-app.git

# 4. ارفع الملفات
git push -u origin main
# (أدخل Username و Token عند الطلب)
```

ثم:
- اذهب لـ Settings → Pages
- اختر `main` branch
- احصل على الرابط!

---

## 🔄 التحديثات المستقبلية

عندما تعدل الملفات وتريد رفع التحديثات:

```bash
cd C:\Users\haith\qudrat-app

# 1. أضف التعديلات
git add .

# 2. احفظها (Commit)
git commit -m "وصف التحديث"

# 3. ارفعها
git push
```

سيتم تحديث الموقع تلقائياً!

---

## ⚠️ حل المشاكل

### المشكلة: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/USERNAME/qudrat-app.git
```

### المشكلة: "Authentication failed"
- تأكد من استخدام Personal Access Token وليس كلمة المرور
- تأكد من نسخ Token بالكامل

### المشكلة: "Permission denied"
- تأكد من اختيار `repo` scope عند إنشاء Token
- جرّب إنشاء Token جديد

---

## 🎯 نصائح مهمة

1. ✅ **احفظ Token** في مكان آمن (ستحتاجه لاحقاً)
2. ✅ **لا تشارك Token** مع أحد (مثل كلمة المرور)
3. ✅ **اختر Public** إذا أردت نشر المشروع للجميع
4. ✅ **GitHub Pages مجاني** تماماً!

---

## 📸 الخطوة التالية

بعد الرفع:
1. شارك الرابط مع الطلاب
2. اطلب منهم تثبيت التطبيق على أجهزتهم
3. اجمع الملاحظات والتحديثات

---

## 🆘 هل تحتاج مساعدة؟

إذا واجهت أي مشكلة:
1. تحقق من رسالة الخطأ بالضبط
2. ابحث في Google: "github error message"
3. أو اطلب المساعدة وأنا جاهز!

---

**🎉 حظاً موفقاً في رفع المشروع!**
